'use strict';

const bookService = async (req, res, next) => {
  // book a service from services list from a certain barber
};

const deleteService = async (req, res, next) => {
  // delete an already booked service};
};

module.exports = {
  bookService,
  deleteService,
};
