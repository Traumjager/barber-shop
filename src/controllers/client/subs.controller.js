'use strict';

const addBarber = async (rq, res, next) => {
  // subscribe a barber
};

const removeBarber = async (rq, res, next) => {
  // remove subscription from a barber
};

module.exports = {
  addBarber,
  removeBarber,
};
