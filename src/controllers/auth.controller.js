const signIn = async (req, res, next) => {
  // sign in with email and pass only
};

const signUp = async (req, res, next) => {
  // sign up with email
  // save the new client/barber
};

module.exports = {
  signIn,
  signUp,
};
